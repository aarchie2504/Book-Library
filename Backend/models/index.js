// Legacy model index — kept for reference only.
// The application now uses the unified User model (models/User.js),
// Book model (models/Book.js), Rating model (models/rating.JS),
// Borrow model (models/Borrow.js), and Settings model (models/settings.js).
// This file is intentionally left as a stub to avoid import errors.
export default {};
